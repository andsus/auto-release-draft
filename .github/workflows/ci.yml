name: CI
on:
  push:
    branches:
      - main

env:
  user_type: "rookie"
  user_experience: "2 weeks"

  # workflow_dispatch:
  #   inputs:
  #     name:
  #       description: 'username'
  #       required: true
  #       default: 'andsus'

jobs:
  build:
    name: CI jobs
    runs-on: p50wsl
    #self-hosted
    #runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest]
    steps:
      - name: Get the sources
        uses: actions/checkout@v2      
      
      - name: Run multiple-line
        run: |
          echo ${{ secrets.AZURE_SECRET_USER }}
          # echo User type ${{ user_type }}!! You have ${{ user_experience }} experience.
